spring:
  application:
    name: incentive-service
  profiles:
    active: local
  kafka:
    url: localhost:9092
  config:
    import: optional:configserver:http://localhost:18080
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server



#  datasource:
#    url: jdbc:postgresql://localhost:5432/ohdelivery
#    username: root
#    password: root
#    driver-class-name: org.postgresql.Driver
#
#  jpa:
#    database-platform: org.hibernate.dialect.PostgreSQLDialect
#    hibernate:
#      ddl-auto: update
#    properties:
#      hibernate:
#        default_schema: INCENTIVE_SCHEMA
#        format_sql: true
#        show_sql: true
#
#  sql:
#    init:
#      mode: always
server:
  port: 19093

eureka:
  client:
    service-url:
      defaultZone: http://localhost:19090/eureka/